# Шаблон переменных окружения
# Скопируйте в .env и заполните значения

# Anthropic Messages API. Получить: https://console.anthropic.com
ANTHROPIC_API_KEY=

# Модель и лимиты
ANTHROPIC_MODEL=claude-haiku-4-5
MAX_SEARCH_USES=4
MAX_OUTPUT_TOKENS=2048

# Контроль расхода. MAX_DAILY_CALLS — жёсткий предел HTTP-вызовов /v1/messages
# в сутки. Поиски идут внутри вызова, поэтому 50 вызовов — это до 50 дайджестов.
# Поисковые сборы: MAX_DAILY_CALLS × MAX_SEARCH_USES × $0.01 = $2.00/сут.
# Токеновая часть этим лимитом не ограничена — только биллинг-алертом.
MAX_DAILY_CALLS=50
RATE_LIMIT_PER_MIN=5
RATE_LIMIT_PER_HOUR=30
CACHE_TTL_HOURS=6

# Сервер
PORT=8080
PUBLIC_HOSTNAME=
