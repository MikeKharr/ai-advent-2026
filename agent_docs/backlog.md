# Бэклог

Очередь задач. Закрытые пункты переносить в `backlog-closed.md`. Лимит 300 строк, проверяется в CI.

## Next

- [ ] Владельцу: принять или отклонить ADR `2026-09-07-1910-model-haiku-4-5.md` (модель) и `2026-09-07-1954-web-search-strict-json.md` (форма вызова).
- [ ] ADR по трактовке I-7 «рабочая ссылка»: HTTP-проверка неприменима — издания отдают 403/405 на HEAD и GET. Принимает владелец. **Предшествует ядру дня 1**: иначе код реализует проверку, смысл приёмки которой не определён.
- [ ] Владельцу: сверить величину биллинг-алерта с измеренными числами — ожидаемая полная загрузка ≈$5.60/сут, структурный потолок ≈$12.50/сут.
- [ ] День 1, ядро: `anthropic.js`, `digest.js`, `cache.js`, `limits.js` + тесты на моке API. Начинать после принятия ADR выше. Обязательное: валидация источников (URL из выдачи поиска, не страница-раздел, у карточек разные), сверка даты с `page_age`/`cited_text` (I-9), счётчики снятых карточек по причинам в логе, схема `publish_digest` на 0–3 карточки с обязательным `note` при неполной выдаче (I-8), запрет добора повторным вызовом.
- [ ] Перед UI дня 1: повторный замер с валидатором — числа снятых карточек по трём причинам. Пустая выдача после валидации переводит на вариант 3 или Sonnet 5 без нового ADR (ADR `2026-09-07-1954`).

## Soon

- [ ] `limits.js`: при `RATE_LIMIT_PER_HOUR=30` два адреса выбирают суточный бюджет меньше чем за два часа, а кэш обходится вариацией строки сферы. Пересмотреть соотношение лимитов до публичного запуска.
- [ ] День 1, сервер и UI: SSE-прогресс поиска, три карточки с источниками, честное пустое состояние.
- [ ] Синхронизация решений в Google Drive по `guides/drive-sync.md`.

## Later

- [ ] Скрипт добавления нового дня: каталог, запись в `Caddyfile`, сервис в `compose.yml`, ссылка на лендинге. Ручной шаг, который забудут.
- [ ] Наблюдаемость: доступ к логам без ssh, алерт при падении контейнера.
- [ ] Скриншот-проверка страниц дней в CI. Баги вёрстки от 2026-09-07 не поймали ни линтер, ни `/healthz`. Осмысленно после появления настоящего UI.
- [ ] Следить за памятью: контейнер на день, 1 ГБ упрётся примерно к десятому дню.
- [ ] Перенести удачные практики обратно в проект HBK.SG.

## Won't do

- Аутентификация и учётные записи — задания этого не требуют.
- База данных сверх локального SQLite-кэша.
- Аналитика и трекеры на публичных страницах.
- Поддомен на день — владелец задал адреса с префиксом пути.
