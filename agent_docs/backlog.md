# Бэклог

Очередь задач. Закрытые пункты переносить в `backlog-closed.md`. Лимит 300 строк, проверяется в CI.

## Next

- [ ] Владельцу: `GET /api/chat` дня 7 не проходит лимитер. Денег не стоит, но это неограниченный поток обращений к базе диалогов с публичного адреса.
- [ ] Владельцу: дни 6 и 7 ходят к роутеру по одному ключу приложения, а суточные счётчики вызовов у них разные — суммарно до 100 вызовов вместо 50. Жёсткий потолок остаётся у роутера ($2 в сутки).
- [ ] Владельцу: `sessions.clear` удаляет строки, но файл базы не сжимается — удалённый текст остаётся на томе до перезаписи страниц. Решить, достаточно ли этого для обещания «вернуть будет нельзя».

- [ ] Владельцу: принять или отклонить ADR `2026-09-07-1910-model-haiku-4-5.md` (модель).
- [ ] Замерить расход за сутки работы и сверить с ожиданием (~$0.011 за запрос дня 1, ~$0.02 дня 2).

## Soon

- [ ] Владельцу: подписи в видах «2 шага» не держатся при росте графа (после #67 — 9 пропусков на каждой ширине, с #70 — 10; канва 538, ширины 1168/1280/1440/1600). Числа дрейфуют от каждого PR: по замеру reviewer на main после #68 в «2 шагах» снова 2, с #70 — 3. Любой новый узел или ребро в главной компоненте переставляет силовую раскладку целиком. Решить: принять как дрейф или чинить расстановку/устойчивость раскладки отдельным PR (класс B).

- [ ] Атлас: порядок `Tab` в двух колонках и на узком экране расходится с экраном — в DOM «Узлы в этом виде» стоят до панели, на экране после (класс B, `design`). Одним порядком DOM не лечится: трём колонкам нужен список до панели. Источник — `agent_docs/design/2026-09-14-1500-atlas-focus-after-step.md`, «Находки», п. 1. Раскладка готова (#80, `design/2026-09-14-1700-atlas-tab-order.md`), код в работе после сбоя загрузки.

- [ ] Атлас: на узком экране ошибку загрузки не видно и не слышно — текст `#canvas-msg` и «Попробовать снова» лежат в скрытом блоке канвы (класс B, `frontend`). Источник — `agent_docs/design/2026-09-14-1500-atlas-focus-after-step.md`, «Находки», п. 2.

- [ ] Атлас: на узком экране после пропуск-ссылки заголовок панели закрыт липкой полосой «Карта» — верх `#panel` на 0, полоса 0…47, `h2` на 25…50 (класс B, `frontend`). Источник — `agent_docs/design/2026-09-14-1700-atlas-tab-order.md`, «Находки», п. 1.

- [ ] Атлас: в панели «Узла нет в этой сборке» кнопка «К началу» вплотную к тексту, зазор 0 px при 360; предложен отступ `--s-3` (класс B, `frontend`). Источник — `agent_docs/design/2026-09-14-1730-atlas-load-error.md`, «Находки», п. 5.

- [ ] Атлас: у загрузки `graph.json` нет срока ожидания — при молчащем сервере «Читаю схему проекта…» висит до таймаута браузера, без «Попробовать снова»; решить срок и что показывать после него (класс B, `design`). Источник — `agent_docs/design/2026-09-14-1730-atlas-load-error.md`, «Находки», п. 6.

- [ ] Выкатка: каталог с `\n`, `"` или `\` в имени пропускается молча (git берёт путь в кавычки даже с `core.quotePath=false`) — безопасная сторона, имя до SSH не доходит; громкая ошибка — `ls-tree -z` в `deploy-units.sh` и в шаге CI (класс A). Находка reviewer и compliance к #77.

- [ ] Атлас: поводы перерисовки до загрузки графа (класс B, `frontend`). (а) Смена `#узел` до загрузки падает в `fromHash` на `state.addresses.get` при `null` — исключение в консоли. (б) Флажок «Весь граф» (`#full`) во время загрузки не `disabled`: если его отметить, объявляется «Вид: » с пустой строкой; выбор запоминается и применяется при `ready`. Найдено при исправлении ложной ошибки загрузки (PR #78) и в его ревью.

- [ ] Владельцу: ограничить окружение `production` в GitHub веткой `main` (Settings → Environments → production → Deployment branches). Сейчас `gh workflow run deploy.yml --ref <ветка>` выкатит код ветки. Находка compliance N4 к #71.

- [ ] Владельцу: приемлем ли возврат слота лимитера при отказах, не дошедших до провайдера (пустой архив, не хватило предела модели). Денег такие отказы не стоят, но повторять их можно без ограничения — работает только суточный счётчик вызовов, а окна 5/мин и 30/час обходятся. Найдено compliance на дне 6; поведение предсуществующее, вариант закрытия — отдельный дешёвый счётчик отказов.

- [ ] Владельцу: решить, пускать ли `summarize`, `translate` и `extract_json` на ярус `cloud-cheap` (модели Groq дешевле Haiku). Сейчас ярус убран из этих классов, чтобы появление моделей Groq не увело их молча; возврат — одна строка в `classes.json`.

- [ ] **Личность дня доходит до роутера.** Роутер ведёт лимиты по приложениям и делает это правильно — но дни 6, 7 и 8 ходят к нему не сами: за них ходит сервис агентов одним приложением `agents`. Роутер видит одного клиента там, где их три, поэтому бюджет и общий. Чинить не ключами по дням (тогда секретов станет по числу дней), а тем, чтобы вызывающий передавал, за кого он ходит, и роутер вёл счёт по этому измерению. Заодно снимет `MAX_DAILY_CALLS` в памяти дня. Поставлено владельцем 2026-09-13; текущий общий бюджет $2 оставлен как есть до этой работы.

- [ ] Суточный счётчик вызовов дня живёт в памяти процесса и обнуляется при перезапуске контейнера. Терпимо при итерации за $0,02, спорно при $0,06. Долговечный стоп — только журнал роутера.
- [ ] Серверное умолчание числа статей снято из реестра агента: вызов без этого поля набирает подборку под предел модели. Дни 6 и 7 присылают своё, но предохранителя на стороне сервиса больше нет. Решить, возвращать ли.
- [ ] Проверка дизайна в CI: контраст палитры и значения вне шкал корпуса. Сейчас держится только на ревью `/design-review`.
- [ ] Лимитер новых дней: отказ API с кодом 4xx (кроме 429) слот не возвращает, поэтому ошибка конфигурации выжигает суточный бюджет за 50 запросов. Решить, возвращать ли слот, когда запрос заведомо не оплачен.
- [ ] Развести два смысла `MAX_OUTPUT_TOKENS` для новых дней: в day1 это `max_tokens` вызова, в day2 — потолок пользовательского ввода; общий `secrets.env` связывает их молча (например, `dayN.env`).
- [ ] Соотношение лимитов в `limits.js` для новых дней: при `RATE_LIMIT_PER_HOUR=30` два адреса выбирают суточный бюджет меньше чем за два часа. Пересмотреть, прежде чем копировать модуль в следующий день.
- [ ] Синхронизация решений в Google Drive по `guides/drive-sync.md`.

## Later

- [ ] Роутер: админ-интерфейс лимитов вместо правки `apps.json` коммитом; реестр провайдеров на хартбитах (`2026-09-08-1020`) за тем же швом `list()`.
- [ ] Роутер: перевести `MAX_DAILY_CALLS` новых дней на лимиты роутера, когда дни станут его клиентами (сейчас — двойная защита).
- [ ] Скрипт добавления нового дня: каталог, запись в `Caddyfile`, сервис в `compose.yml`, ссылка на лендинге. Ручной шаг, который забудут.
- [ ] Наблюдаемость: доступ к логам без ssh, алерт при падении контейнера.
- [ ] Скриншот-проверка страниц дней в CI. Баги вёрстки от 2026-09-07 не поймали ни линтер, ни `/healthz`.
- [ ] Следить за памятью: контейнер на день, 1 ГБ упрётся примерно к десятому дню.
- [ ] Перенести удачные практики обратно в проект HBK.SG.

## Won't do

- **Доработка и поддержка приложений сданных дней** (`days/day1`, `days/day2`, …) — решение владельца от 2026-09-08. Новые стандарты применяются к новым дням, ретроспективно старые не приводятся. Возобновляется только по явному указанию владельца. Сюда перенесены: ретрофит дней 1–2 под дизайн-корпус; второй китайский источник для дня 1; перенос в day1 атомарного `limiter.reserve` и `.env*` в `.dockerignore`; защита `/api/answer` дня 2 от форжённых запросов (`Sec-Fetch-Site`).
- Аутентификация и учётные записи — задания этого не требуют.
- База данных сверх локального SQLite-кэша.
- Аналитика и трекеры на публичных страницах.
- Поддомен на день — владелец задал адреса с префиксом пути.
