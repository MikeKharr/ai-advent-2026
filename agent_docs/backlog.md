# Бэклог

Очередь задач. Закрытые пункты переносить в `backlog-closed.md`. Лимит 300 строк, проверяется в CI.

## Next

- [ ] Владельцу: подтвердить стек — Node 22 без зависимостей или Hono + TypeScript. Блокирует начало разработки.
- [ ] Владельцу: создать публичный репозиторий `MikeKharr/ai-advent-2026` и запушить каркас.
- [ ] Владельцу: выпустить `ANTHROPIC_API_KEY`, поставить биллинг-алерт в консоли.
- [ ] Спайк: `web_search` + строгий JSON одним вызовом. Проверить гипотезу «web_search + client tool со `strict: true`»; запасной вариант — два вызова. Результат оформить ADR.

## Soon

- [ ] Ядро: `anthropic.js`, `digest.js`, `cache.js`, `limits.js` + тесты на моке API.
- [ ] Сервер и UI: `server.js`, SSE-прогресс поиска, три карточки с источниками.
- [ ] CI для кода: typecheck, тесты, запрет мержа при красном.
- [ ] Владельцу: создать инстанс Vultr (Singapore, High Frequency, Ubuntu 24.04), сообщить хост.
- [ ] Упаковка: Dockerfile, compose.yml, Caddyfile, bootstrap.sh.
- [ ] Деплой через GitHub Actions по push в main.

## Later

- [ ] Синхронизация решений в Google Drive по `guides/drive-sync.md`.
- [ ] Проверка живого деплоя по чек-листу верификации.
- [ ] Перенести удачные практики обратно в проект HBK.SG.

## Won't do

- Аутентификация и учётные записи — задача этого не требует.
- База данных сверх локального SQLite-кэша.
- Аналитика и трекеры на публичной странице.
- Собственный публичный API.
