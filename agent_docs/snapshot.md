# Снимок текущего состояния

Обновлять в конце каждой итерации. Лимит 300 строк, проверяется в CI.

## Где сейчас работа

Инициализация репозитория. Каркас по `2030ai/2030ai-project-template` с memory-слоем `cmit-ru/cmit-starter` создан. Прикладного кода ещё нет.

## Что готово

- Каркас документации `agent_docs/` и правила `AGENTS.md`.
- 10 ролей агентов в `.claude/agents/`.
- CI: markdownlint, docs-guard (лимит 300 строк, имена атомарных документов, проверка на утечку ключа).
- План задачи 1 согласован: приложение «сфера → 3 новости», Node 22, Vultr Singapore, Caddy.

## Чем заниматься дальше

Первым — спайк `web_search` + строгий JSON (см. `backlog.md`, раздел Next). Это единственное, что может заставить переделать архитектуру, поэтому проверяется до UI.

## Открытые вопросы

- **Стек не подтверждён окончательно.** Предложен Node 22 без зависимостей вместо Hono + TypeScript, потому что в песочнице агента заблокированы npm и PyPI и код нельзя было бы запустить перед передачей. Ждёт решения владельца. См. `backlog.md` → Next.
- Хост и A-запись не назначены.
- Инстанс Vultr не создан.

## Заблокировано

- Создание репозитория на GitHub — у агента токен без прав на создание репозиториев. Делает владелец.
- Провижининг VPS — из песочницы агента исходящий SSH закрыт. Делает владелец по `bootstrap.sh`.
