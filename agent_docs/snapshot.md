# Снимок текущего состояния

Обновлять в конце каждой итерации. Лимит 300 строк, проверяется в CI.

## Где сейчас работа

**Фреймворк v2 принят и внедрён 2026-09-10** (ADR `2026-09-10-0426`):
модели и усилие — свойство ролей, skills Османи предзагружены в роли, гейты
мержа — по классу изменения A/B/C, оркестрация — скилл `/day-cycle`,
разрешения — в `.claude/settings.json`. Сессии запускать из каталога
репозитория (`guides/environment-setup.md`, правило 0).

**SDLC изменён по ADR `2026-09-11-0513`:** мерж и выкатку делает агент при
консенсусе гейтов (`guides/agent-roles.md`, «Мерж по консенсусу»); за
владельцем — принятие ADR, PR с защищёнными путями и откат прода. Объём —
под контролем владельца (`AGENTS.md`, «Объём»). Витрины Drive нет.
PR #97 смержен: атлас отражает новый процесс (фаза выкатки без гейта владельца).

**Модели в ролях — полными id** (#108): `architect` —
`claude-fable-5-1`/high, остальные — `claude-opus-5[1m]`; `design` — medium
(ADR `2026-09-11-1105`). На новую версию модели роли сами не перейдут.
**Список `ask` в `.claude/settings.json` убран** (#112, ADR
`2026-09-11-1154`): shell-команды не ждут клика; force-push в `main`
закрыт защитой ветки. **Правило ssh в `/day-cycle`** — ADR
`2026-09-11-1230` (#115): закрытый список серверных команд дословно в
`/day-cycle` вместо окна; `exec` в контейнеры прода — только владелец.
Предмет списка — что агент запускает по ssh сам; исполнение в контейнере
прода из шага выкатки под его запреты не подпадает (ADR `2026-09-22-1301`).
**PR с защищёнными путями сливает агент (всегда класс A), копию
существующего `AGENT_KEY` из `agents.env` в `dayN.env` делает агент** —
ADR `2026-09-11-1743` (PR #122, «Принято», решение владельца в чате
2026-09-12); за владельцем — принятие ADR, новые секреты, откат.

**Даты в именах документов — реальные, по UTC** (ADR `2026-09-11-1046`,
PR #110 и #111): 57 файлов с именем «проектных часов» названы по времени мержа
своего PR, таблица старых имён —
`development-history/2026-09-11-1104-rename-future-dated-docs.md`.
Новые имена — из `date -u +%Y-%m-%d-%H%M` (`guides/atomic-documents.md`);
`docs-guard` проверяет формат во всех трёх каталогах и что время не в
будущем. С #114 выкатка проверяет `/healthz` каждой единицы до 12 раз
с паузой 4 с
(`development-history/2026-09-11-1507-deploy-healthz-ssh-norm-key-samples.md`).

**Ключ в документе роняет сборку** (#116, класс A): единый список
`KEY_SAMPLES` (PEM, `gh[pousr]_`, `github_pat_`, `gsk_` и `sk-ant-` с
хвостом) — находка сборки атласа и `--check`; шаг секретов `docs-guard`
ищет тот же список, пишет в журнал только имена файлов, код 2 у `grep` —
красный.

**День 8 выкачен и проверен в проде 2026-09-09** (PR #45): релевантность статей — по репликам разговора (`development-history/2026-09-09-2135-day8-dialogue-relevance.md`).

**Выдача роли `architect` — решения, а не энциклопедии** (ADR
`2026-09-19-1252`, #171, принят в два приёма): шесть признаков текста,
блокирующие — 1, 3 и 5; проверяет `reviewer`; правило внесено в
`.claude/agents/architect.md` и `guides/agent-roles.md`
(`development-history/2026-09-20-0236-architect-output-norm-gates.md`).

**Вход на сервер, сайт `zpq.ai` и почта живут в приватном `MikeKharr/zpq-ai`**
(ADR `2026-09-22-0611`). **Переключение выполнено 2026-09-22, прод на новом входе:**
80, 443 и сертификаты четырёх имён держит проект `zpq`, Caddy адвента внутренний
(`http://challenge.zpq.ai`, без портов) на сети `edge` под алиасом `advent`. Четыре тома
(`advent_caddy_data`, `advent_caddy_config`, `advent_mail_etc`, `advent_mail_data`)
перешли к `zpq` как `external`, без копирования: сертификат `challenge.zpq.ai` — прежний,
выпуска 2026-09-07, то есть **ни одного нового выпуска переезд не сделал**. Адрес
посетителя через два прокси держат `trusted_proxies static private_ranges` и `header_up
X-Forwarded-For {client_ip}` во **всех четырнадцати** блоках `deploy/Caddyfile`; промах
хотя бы в одном — лимитер того дня схлопывается на один адрес для всех, поэтому новый
день без этой строки не добавлять (`guides/new-day.md`). Подробности —
`development-history/2026-09-22-1056-split-zpq-entry.md`; записи о сайте остаются здесь
(`development-history/2026-09-18-0959-zpq-site-launch.md`, `development-history/2026-09-18-2042-zpq-cli-look.md`), находки — в бэклоге `zpq-ai`.
**Правку `deploy/Caddyfile` с 2026-09-22 применяет сама выкатка:** конфиг
примонтирован файлом и контейнер не пересоздаётся, поэтому шаг сверяет сумму
файла с последней **удачно применённой** и при расхождении делает
`caddy validate` и `caddy reload` без разрыва слушателей
(`development-history/2026-09-22-1244-deploy-caddy-reload.md`). Проверено двумя
живыми прогонами (#201 и #199); ручной `restart caddy` больше не нужен.

**Почта `mail.zpq.ai` (Stalwart) переехала в `zpq-ai` на тех же томах и отвечает
302 на `/`** — до переезда было 502. Диагностика и починка теперь там, где разрешён
`exec`; туда же уехали девять пунктов «Владельцу» и три стоячих условия `compliance`
(порт 25 не открывается, пока журналирование не доказано отрицательным тестом).
`restart mail` удалён из закрытого списка как мёртвая строка; тем же решением в список
добавлен каталог `cd zpq-ai/deploy` — `ps`, `logs`, `restart caddy` (ADR `2026-09-22-1824`).

**Атлас проекта: все четыре этапа в `main`, витрина в проде на
`challenge.zpq.ai/atlas/`** (ADR `2026-09-10-0550`, PR #51, #52, #54, #56).
Сборка без зависимостей строит из репозитория `dist/graph.json`,
`dist/vault/` (заметки Obsidian) и `dist/site/` — витрину схемы. Команды —
`atlas/README.md`. Шаг `--check` — в обязательной проверке `guard`, так что
битая ссылка между документами роняет CI. Подробности — записи
`2026-09-10-0806`, `2026-09-10-1240` и `2026-09-10-1338`.
Витрина выкатывается отдельной единицей `atlas`; любой docs-мерж
пересобирает её. `detect` в `deploy.yml` считает изменения от последней
успешной выкатки, а не от `HEAD^`, поэтому правило «не мержить, пока выкатка
ждёт» снято (#71, `operations.md`). Откат прода — действие
владельца, агент перезапускает только последний запуск выкатки
(`development-history/2026-09-11-0043-ci-detect-last-deploy.md`). **Белый
список имён единиц в проде** (#73, #77, #84, #87, #89, класс A): единица —
только `[a-z0-9]+` с `Dockerfile` в `head`, имя вне списка и сбой git —
громкая ошибка, а не молчаливый пропуск; тесты на PR выбирают те же единицы,
что выкатка
(`development-history/2026-09-11-0123-ci-deploy-unit-allowlist.md`).

**Атлас после этапов — 2026-09-10/11** (#64…#88, #101, #104—#106; ADR
`2026-09-11-0745`): всегда объём, весь граф, полнотекстовый поиск; подробности —
записи `2026-09-10-1602`…`2026-09-11-0321` и `2026-09-11-0904`. Страница
прогресса — главная `challenge.zpq.ai` (#63, #100): дни, атлас, «Сейчас в
работе», журнал PR; `/progress/` отдаёт 404. Данные — `site/data.js`, правит
`docs` в конце итерации (#103, `2026-09-11-0903-home-is-progress-page.md`).
**День 9 выкачен 2026-09-11** (ADR `2026-09-11-1608`, #118–#121):
`challenge.zpq.ai/day9/` — чат дня 8 со сводкой разговора; сравнение с усечением — `2026-09-11-1758-day9-compression-comparison.md`.
**День 10 выкачен 2026-09-14** (ADR `2026-09-14-0447`, раскладка
`design/2026-09-14-0615-day10-strategies-layout.md`, PR #131–#134):
`challenge.zpq.ai/day10/` — стратегия памяти выбирается параметром запуска.
**Все четыре стратегии ADR в проде**: сводка дня 9, **скользящее окно**
(последние M реплик, умолчание 10), **ветки разговора** (дерево сообщений,
`messages.parent_id`/`sessions.head_id`, навигатор «‹ i/n ›», обзор веток,
`PUT /v1/sessions/:id/head`) и **липкие факты** (свой вызов Haiku после каждого
ответа, умолчание 600 токенов; #136, #137). День 10 сделан двумя заходами в
срочном режиме по решению владельца; ADR не переписывался, дни 6–9 не изменились
(`development-history/2026-09-14-0700-day10-window-branches.md`).
**Переезд атласа на project-atlas выполнен 2026-09-12** (ADR
`2026-09-12-0440`, PR #127 и #128; выкатка прошла, `/atlas/healthz` — 200).
Кода атласа здесь больше нет: инструмент живёт в публичном
`MikeKharr/project-atlas` и приходит в CI клоном на закреплённом теге.
Версия записана в одном месте — `.github/scripts/atlas-tool.sh`
(`TOOL_TAG=v2.0.1`, `TOOL_SHA=27304b80…`); обновление версии — PR класса A
по процедуре раздела 4 ADR. В `atlas/` остались `Dockerfile`, `Caddyfile`,
`overlay.json`, `atlas.config.json` (формат 2) и `README.md`. Цена: `guard`
клонирует чужой репозиторий на каждом PR, а сбой клона в выкатке пропускает
`deploy` для всех единиц запуска
(`development-history/2026-09-12-1401-atlas-migration-to-project-atlas.md`).

**Провайдер Kimi подключён 2026-09-15** (ADR `2026-09-15-1448`, #140–#143):
адаптер `router/src/adapters/kimi.js`, четыре модели на общем `KIMI_API_KEY`,
`explicitOnly` — политика сама такого провайдера не берёт. `dataClasses` у всех
пяти провайдеров полный: поле описывает оркестрацию «тип данных × мощность
модели», а не границу выхода данных (решение владельца 2026-09-15, уточнение
под п. 5 ADR `2026-09-08-1748`). **Четыре модели Kimi доступны на дне 11**
(ADR `2026-09-16-1038`, #158, #159): второй закрытый список `LAYERED_MODELS` в
`agents/src/params.js`, дни 5–10 его не видят. Поле «Лимит ответа» на моделях с
рассуждениями границу не держит: заказано 100 токенов, получено 296
(`development-history/2026-09-16-1605-kimi-models-on-day11.md`).

**День 11 выкачен и замерен в проде 2026-09-16** (ADR `2026-09-15-2024`,
раскладка `design/2026-09-16-0542-day11-profiles-memory-layout.md`,
PR #145–#154): `challenge.zpq.ai/day11/` — диалоговый агент без новостей, у
которого контекст собирают три слоя памяти. Память персонализации (настройки
профиля плюс правила из разговора) и память фактов по темам крепятся к
**профилю** и живут 30 дней; рабочая память — к сессии, 30 часов. Новую тему
агент открывает только с согласия человека. Профили открыты: любой посетитель
может занять, прочитать, пополнить и удалить любой из пяти. Класс роутера
`layered_dialogue` (#148) и оба потолка приложения `agents` (#146) выкачены
до сервиса. **Замер дешевле оценки ADR:** $0,0024 против оценки $0,007–0,009.
**Расход роутера агент читает сам** — `router/admin.js` двумя строками в
закрытом списке команд (ADR `2026-09-16-0907`, #155, #156)
(`development-history/2026-09-16-0953-day11-profiles-memory-layers.md`).
Находки замера и гейтов — в `backlog.md`, «Владельцу».

**День 13 выкачен и проверен живьём в проде 2026-09-22** (ADR `2026-09-21-1747`,
раскладка `design/2026-09-21-1852-staged-run-layout.md`, PR #181–#187; дня 12 нет):
`challenge.zpq.ai/day13/` — `staged-agent` ведёт ответ шестью этапами, пауза
обрывает вызов провайдера на месте (#184), журнал проходов — CSV; профили и
хранилище общие с днём 11. Дефект возобновления после закрытого окна починен и
подтверждён платной прогонкой. Тестов 64, живая связка —
`agents/test/day13-reconnect.test.js`; детали — записи `2026-09-22-0442`,
`-0625`, `-0720`, `-0815`.

**День 14 выкачен и проверен живьём в проде 2026-09-22** (ADR `2026-09-22-0827`
и `2026-09-22-1221`, PR #200, класс A; раскладки отдельной фазой не было —
решение владельца): `challenge.zpq.ai/day14/` — память профиля пополнилась
**инвариантами**: короткие правила до 200 знаков с номерами `П1`, номер от
кода и не переиспользуется, 10 на профиль. Заведение — только через ворота
формулировщика, и держит их сервис билетом HMAC на ключе своего старта (`POST`
без билета — 400). Машина одна на дни 13 и 14, шов — опция `invariants` у
`createStagedAgent`, страж дня 13 — тест на `sha8` его промптов. Гарантия одна:
третья строка вердикта на `verify` и её исполнение на `deliver`; названное
нарушение на последнем круге даёт неотданный ответ. Прогоны: `agents` 286,
`days/day14` 92. Живая проверка: дефектов нет, $0,0045 за сценарий (часть —
оценка); детали и уроки — записи `2026-09-22-1120` и `2026-09-22-1248`,
находки — `backlog.md`. **С #204 неотданный ответ объясняет выход:** под
репликой «Ответ не отдан» названы три выхода (спросить иначе, смягчить или
удалить инвариант, поднять предел кругов), третий — только пока предел есть
куда поднимать, на потолке текст говорит, что выше не поднять; сам запуск
больше не объявляется удачным. Уроки трёх PR — запись `2026-09-22-1419`.

**Утверждение о защищённых путях в описании PR сверяется с диффом** (#206,
класс A): шаг обязательного задания `guard` читает список путей из
`guides/agent-roles.md` (п. 6, между маркерами `protected-paths`) по
**объединению** базовой и головной версий, поэтому PR не может сузить список и
судиться уже по укороченному. Порча документа даёт красное, а не пустой список;
событие `edited` перекрашивает гейт после правки описания. В списке с #206 и
`CLAUDE.md`. Страж доказывает, что путь **назван**, но не что сказанное о нём
правда — чтение диффа человеком остаётся несущим слоем
(`development-history/2026-09-22-1450-protected-paths-guard.md`).

**Протечка суточного потолка роутера на оборванных вызовах закрыта
2026-09-22** (#189, #190, класс A, выкачено): исход `error` разделён на
`server_error` (любой 5xx — ноль по обоим концам), `bad_response` (успешный код
с неразобранным телом — выход по оценке) и остаточный `error` (выход нулём).
125 тестов в `router/`; правило списания — `operations.md`.

Все выкаченные дни (1–11, 13, 14), роутер и сервис агентов в проде. Роутер
(`router/`, ADR `2026-09-08-1748`) задеплоен 2026-09-08; Groq и класс
`guard_prompt` — там же (`2026-09-08-2130-router-groq-provider`, `operations.md`).
Дни 1–4 к роутеру не переводятся.

## Что готово

- Каркас документации `agent_docs/` и правила `AGENTS.md` по шаблону 2030ai + memory-слой cmit-starter.
- 10 ролей агентов в `.claude/agents/`.
- Топология: `days/dayN/` — контейнер на день, Caddy маршрутизирует `challenge.zpq.ai/dayN`, лендинг на корне.
- `deploy/`: `Caddyfile`, `compose.yml`, идемпотентный `bootstrap.sh`.
- CI: markdownlint, docs-guard, ci (синтаксис, тесты, сборка образа, проверка `/healthz`), валидация `Caddyfile`.
- Деплой: сборка в Actions → GHCR → пин тега на сервере → перезапуск только изменённых дней → проверка живого адреса.
- Репозиторий `MikeKharr/ai-advent-2026` публичен; секреты `SSH_HOST`, `SSH_USER`, `SSH_KEY` заведены.
- Сервер провиженен `bootstrap.sh`, вход по ключу под пользователем `advent` работает.
- Стек принят: ADR `2026-09-07-1525-zero-dependency-node.md` — Node 22 без runtime-зависимостей.
- Ключ Anthropic на сервере (`deploy/secrets.env`, общий на все дни), биллинг-алерт поставлен, branch protection на `main` включена: PR обязателен, `guard` — обязательная проверка, force-push запрещён.
- Скиллы `addyosmani/agent-skills` (25) в `.agents/skills/` с зеркалами-симлинками; используются внутри фаз, процесс 2030ai — верхний уровень (ADR `2026-09-07-2350`).
- Гейт безопасности скиллов: NVIDIA SkillSpector (CLI) + скилл `skill-inspector`; проверка перед любой установкой стороннего скилла обязательна (AGENTS.md, ADR `2026-09-08-0205`). Набор addyosmani просканирован ретроспективно, HIGH-находки объяснены чтением источника.
- Дизайн в пайплайне (ADR `2026-09-08-1110`): корпус `agent_docs/design/corpus.md` (шкалы, палитра с проверенным контрастом, обязательные состояния), собственный скилл `/design-review`, роль `design`. UI проходит через `design` до кода и перед мержем. Метод заимствован у `vercel-labs/eve-design-template` (MIT); сам шаблон — Slack-агент на Vercel — не разворачивался. Действует с дня 3.
- Правило владельца от 2026-09-08: приложения сданных дней не дорабатываются и не поддерживаются без явного указания. Пункты по дням 1–2 перенесены в `backlog.md` → Won't do.
- Расход: `MAX_DAILY_CALLS` ограничивает вызовы на каждое приложение дня отдельно — так и задумано (решение владельца 2026-09-08: общий предел на весь ключ не используют). Потолок по ключу при четырёх днях — порядка $4–8 в сутки (`architecture.md`), сверху биллинг-алерт.
- Домен закрыт от индексации: `X-Robots-Tag: noindex` на всех ответах сайта, включая дни и JSON API; `robots.txt` обход намеренно разрешает — иначе робот не увидит noindex (`development-history/2026-09-08-1245`).
- **День 3 в проде:** в модель уходит полный текст статьи из ленты, а не заголовок — шесть лент из восьми отдают его сами, за страницами статей приложение не ходит (ADR `2026-09-08-1420`). Тематика и запрос в одном блоке, запрос виден в ленте. Бюджет 120K символов текста, статей с источника 1–15 (по умолчанию 5). 56 тестов. Вызов ~$0.033, при максимуме ~$0.06.
- **День 4 в проде:** день 2 плюс температура (ползунок 0–1, шаг 0.1, в ленте отдельным тегом), подсказки сфер на четырёх языках, заголовок и описание переписаны под смысл продукта, лента со своим скроллом. Первый экран по дизайн-корпусу: значений вне шкалы — ноль против 21 в дне 2, `:focus-visible` на всех элементах. 46 тестов. Температура не отправляется при дефолтном положении ползунка — иначе смена общей `ANTHROPIC_MODEL` ломала бы день 400-ми и выжигала суточный лимит.
- **Роутер LLM-провайдеров** (ADR `2026-09-08-1748`, принят): отдельный сервис `router/` —
  выбор по классу задачи (возможность → отказ, политика → порядок, здоровье → пропуск),
  адаптеры Anthropic и Ollama (родной API), ключи провайдеров и приложений у роутера
  (`deploy/router.env`), лимиты на приложение коммитом в `apps.json`, журнал расхода JSONL
  на томе. Эндпоинты `/v1/route`, `/v1/spend`, `/v1/metrics`. CI/deploy обобщены до единиц
  `days/dayN` и `router`. Эксплуатация — `agent_docs/operations.md`.
- **День 1 в проде:** `https://challenge.zpq.ai/day1/` — дайджест из восьми RSS-лент
  (ADR `2026-09-07-2016`), модель выбирает номера, ссылки и даты из лент. Цена запроса
  $0.011, 20 тестов. `/day1/healthz` — 200.
- **День 2 в проде:** `https://challenge.zpq.ai/day2/` — поверх пайплайна дня 1 —
  параметры `max_tokens` (200 по умолчанию), `stop_sequences`, формат в промпт,
  обрезка N статей с источника (по умолчанию 30); лента результатов с токенами
  вход → выход и причиной останова, хранится в localStorage. Ответ — свободный текст,
  ссылки — по белому списку лент (ADR `2026-09-07-2330`, принят).
  38 тестов; проверено на сервере с настоящим ключом: `max_tokens`, `stop_sequence`
  и формат работают, расход ~$0.02 за запрос (~18K входных токенов).

- **День 5 в проде:** `https://challenge.zpq.ai/day5/` — архив статей на сервере
  (окно 1000 с квотой 125 на издание, том `day5_data`), алгоритмический отбор под
  запрос по всему архиву, выбор модели пользователем: Haiku, GPT-OSS 20B или
  Qwen3.6 27B. Первый день, который ходит через роутер и не имеет ключей к моделям.
  Подборка подгоняется под живой остаток квоты провайдера: роутер читает его из
  заголовков ответа и отдаёт приложению через `/v1/models` (ADR
  `2026-09-09-0156`, `2026-09-08-1825`, `2026-09-08-1824`). 41 тест.

- **День 7 в проде:** `https://challenge.zpq.ai/day7/` — чат с агентом вместо
  одиночного запроса. Переписка живёт в SQLite на томе `agents_data`
  (`/data/sessions.db`), сессия определяется cookie от сервера, срок хранения
  30 часов без новых сообщений, уборка при старте и раз в десять минут.
  Хвост диалога уходит модели, действующий размер контекста ограничен долей
  предела входа модели. ADR `2026-09-09-1906`, раскладка
  `design/2026-09-09-1905`. 90 тестов у сервиса агентов, 10 у дня.
  Живая проверка: перезапуск контейнера агента переписку не теряет —
  модель пересказала разговор, бывший до перезапуска.

- **День 6 в проде:** `https://challenge.zpq.ai/day6/` — запрос выполняет
  отдельный сервис `agents` (реестр агентов, инструмент «архив» на томе
  `agents_data`, запуски с потоком событий по SSE), страница дня — пульт к
  нему: монитор работы над лентой ответов и окно «Об агенте» с системным
  промптом. Третий контейнер без публичного адреса; ключа роутера у дня нет,
  деньги тратит агент по приложению `agents`. ADR `2026-09-09-0854`,
  спецификация `design/2026-09-09-0855-day6-monitor-layout.md`. 68 тестов.
  Живая проверка 2026-09-10 на умолчаниях: девять событий за 10.5 с, архив
  до 178 статей за 1.8 с, ссылки все из списка источников.

## Чем заниматься дальше

**Переезд в `zpq-ai` закрыт: PR #199 слит (`bb39d06`) и выкачен, `main` и прод
сошлись.** Секреты выкатки `zpq-ai` заведены, PR #8 там слит (`f59b7a4`) — новый
репозиторий выкатывается сам. Уроки итерации — `development-history/2026-09-22-1723-zpq-split-merged-lessons.md`
и `guides/verification.md` (методика проверки, ADR `2026-09-23-0333` принят); остаётся платная
проверка адреса посетителя через два прокси (≈$0,07) — решение владельца.
Дни 13 и 14 закрыты целиком; правка в `days/day13/**` и `days/day14/**`
поднимает единицу `agents` в отборе тестов CI (#194). Из `backlog.md` — решения владельца
(Next и «Владельцу»); шапка задаёт метку «проверено ДАТА: ЧЕМ» и два вида закрытия (`[x]` выполнено, `[~]` снят как ошибочный), верхние шесть пунктов сверены по коду 2026-09-23.

## Открытые вопросы

- **Суточный бюджет общий на дни 6–10, и день 11 встанет в ту же очередь** — потолок поднят с $2 до $10 (решение владельца 2026-09-16 по доводу compliance на проектировании дня 11). Лимиты работают в паре: одного денежного числа мало, потому что токенный проверяется первым, поэтому вместе с ним `dailyTokens` поднят с 2 до 10 млн. Прежняя пара давала фактический предел около $2–3,6, а не $2 ровно; теперь денежный лимит упирается первым и $10 — настоящий потолок. Общим он быть не перестал: живая очередь, активный день может посадить остальные до полуночи UTC — поднятие лишь отодвигает этот момент. Причина в другом: роутер ведёт лимиты по приложениям, а за все дни к нему ходит одно приложение `agents`. Задача «личность дня доходит до роутера» — в `backlog.md`.
- Покрытие Китая: рабочая англоязычная лента одна (`pandaily`). `techinasia` — 403, `kr-asia` и `36kr` — не RSS, `technode` отдаёт 11.5 МБ архива.
- Разнообразие источников держится отбором по кругу (день 1) и потолком на источник (день 2), а не гарантией: если ленты региона молчат неделю, регион в выдаче не появится.
- Модель: ADR `2026-09-07-1910-model-haiku-4-5.md` в статусе «Предложено». Правки в конфигурации уже внесены по указанию владельца, решение по ADR за ним — вскрылись срок вывода Haiku 4.5 (не ранее 15.10.2026) и неопределённость итоговой стоимости запроса.
- Вопросы дня 2 из бэклога: защита `/api/answer` от форжённых запросов с чужих страниц
  (`Sec-Fetch-Site`) и разведение двух смыслов `MAX_OUTPUT_TOKENS` — решения за владельцем.
- Ноутбук владельца подключён к проду через Tailscale (ADR `2026-09-09-0258`):
  Ollama слушает только частную сеть, провайдер `mac-qwen3` доступен роутеру,
  в дне 5 модель выбирается последней с пометкой «медленно». Пока ноутбук
  в сети, классы `summarize`, `translate` и `extract_json` будут пробовать
  его первым — ярус `self-hosted` стоит в политике впереди облака.

## Заблокировано

Ничего.
