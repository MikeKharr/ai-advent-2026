# Снимок текущего состояния

Обновлять в конце каждой итерации. Лимит 300 строк, проверяется в CI.

## Где сейчас работа

Конвейер прожит целиком: деплой заглушки дня 1 на сервер прошёл, TLS работает. Стек принят (Node 22 без зависимостей). Прикладного кода нет — следующий шаг спайк `web_search` + строгий JSON.

## Что готово

- Каркас документации `agent_docs/` и правила `AGENTS.md` по шаблону 2030ai + memory-слой cmit-starter.
- 10 ролей агентов в `.claude/agents/`.
- Топология: `days/dayN/` — контейнер на день, Caddy маршрутизирует `challenge.zpq.ai/dayN`, лендинг на корне.
- `deploy/`: `Caddyfile`, `compose.yml`, идемпотентный `bootstrap.sh`.
- CI: markdownlint, docs-guard, ci (синтаксис, тесты, сборка образа, проверка `/healthz`), валидация `Caddyfile`.
- Деплой: сборка в Actions → GHCR → пин тега на сервере → перезапуск только изменённых дней → проверка живого адреса.
- Репозиторий `MikeKharr/ai-advent-2026` публичен; секреты `SSH_HOST`, `SSH_USER`, `SSH_KEY` заведены.
- Сервер провиженен `bootstrap.sh`, вход по ключу под пользователем `advent` работает.
- A-запись назначена, TLS выдан: `https://challenge.zpq.ai/` — 200, `/day1/healthz` — 200 (`{"ok":true,"day":1,"node":"v22.23.2"}`), образ дня 1 запинен на тег коммита.
- Стек принят: ADR `2026-09-07-1525-zero-dependency-node.md` — Node 22 без runtime-зависимостей. Разработка разблокирована.

## Чем заниматься дальше

Спайк `web_search` + строгий JSON одним вызовом Messages API — единственное, что может заставить переделать архитектуру дня 1. Затем ядро дня 1.

## Открытые вопросы

- Нет открытых вопросов по инфраструктуре; следующий риск — совместимость `web_search` со structured outputs (снимается спайком).

## Заблокировано

- Запуск спайка и всё, что вызывает API: `ANTHROPIC_API_KEY` ещё не выпущен и не положен в `deploy/day1.env` на сервере. Делает владелец.
