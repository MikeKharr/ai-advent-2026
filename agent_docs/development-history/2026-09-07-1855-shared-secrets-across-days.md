# [2026-09-07 18:55] Один файл секретов на все дни + гайд по новому дню

Файл: `agent_docs/development-history/2026-09-07-1855-shared-secrets-across-days.md`

## Что сделано

- `deploy/secrets.env` — общий файл секретов для всех дней. Сервис дня подключает его
  через `env_file` в `compose.yml` наравне с необязательным `dayN.env`, который теперь
  нужен только для настроек, специфичных для конкретного дня.
- `deploy/put-secrets.sh` — скрипт заведения и ротации ключа: спрашивает ключ скрытым
  вводом и передаёт его на сервер через stdin ssh, так что ключ не пишется на диск
  локальной машины, не попадает в историю shell, в список процессов и в вывод. Файл
  создаётся под `umask 077`; если он уже есть — заменяется только строка с ключом,
  остальные значения сохраняются (иначе ротация молча откатывала бы лимиты, I-5).
- `agent_docs/guides/new-day.md` — порядок добавления дня N: каталог, две строки в
  `Caddyfile`, сервис в `compose.yml`, ссылка на лендинге, деплой и откат. Первым пунктом
  зафиксировано, что с секретами для нового дня делать ничего не нужно.
- Ссылки на гайд из `architecture.md` и `index.md`; текст подсказки `bootstrap.sh` и
  `deploy/.env.example` приведены к общему файлу.

## Зачем

Изначально секреты были расписаны по файлу на день (`day1.env`), то есть при каждом новом
задании владельцу пришлось бы повторять процедуру с ключом вручную. Ключ на все дни один,
поэтому файл сделан общим: заведение и ротация — один запуск скрипта, новый день не требует
никаких действий с секретами.

Проверено: новый `compose.yml` скопирован во временный каталог сервера и принят
`docker compose config` (локально docker не установлен); обе ветки скрипта — создание и
ротация — прогнаны на сервере на тестовом файле с фиктивным ключом, значения при ротации
сохранились. Гайд сверен с фактическим устройством репозитория: `<base>` в дне 1 намеренно
убран (`2026-09-07-1900-base-tag-and-landing-fix.md`), пакет GHCR для нового дня нужно
делать публичным вручную, иначе `docker compose pull` уронит первый деплой.

## Обновлено

- [x] agent_docs/architecture.md
- [x] agent_docs/index.md
- [x] agent_docs/guides/new-day.md (новый)
- [x] deploy/compose.yml, deploy/.env.example, deploy/bootstrap.sh, deploy/put-secrets.sh
- [x] deploy/Caddyfile — комментарий требовал `<base href="/dayN/">`, что противоречит
      решению из `2026-09-07-1900-base-tag-and-landing-fix.md`
- [ ] Тесты — неприменимо: изменение конфигурационное, проверено `docker compose config`

## Связанные решения

- `agent_docs/adr/2026-09-07-1610-monorepo-per-day-path-routing.md`
- Инварианты I-1, I-2, I-3 (ключ живёт только на сервере, не попадает в репозиторий, ротируется)
