# [2026-09-23 07:01] День 15 заведён каталогом: копия дня 14 и подключение к деплою

Файл: `agent_docs/development-history/2026-09-23-0701-day15-bootstrap.md`

## Что сделано

- `days/day15/` — копия `days/day14/` с заменой номера и ничем больше:
  `diff -r days/day14 days/day15` даёт только строки, где `14` стало `15`
  (cookie `day15_pid` / `day15_sid`, `COOKIE_PATH=/day15/`, имена файлов
  `day15-stages-*.csv`, заголовок страницы, README, комментарии и тесты).
  Вывод `diff -r` целиком — в описании PR.
- Подключение к деплою: служба `day15` в `deploy/compose.yml` (образ
  `ghcr.io/mikekharr/advent-day15`, `day15.env` необязательным файлом,
  `depends_on: agents`) и в `depends_on` входа; `redir` и `handle_path`
  в `deploy/Caddyfile`; описание `deploy/day15.env` в `deploy/.env.example`;
  карточка дня и строка про роутер и сервис агентов в `site/index.html`.
- `.github/workflows/ci.yml` — исключение отбора единиц расширено на
  `days/day15/**`: правка дня поднимает ещё и `agents`, как у дней 13 и 14.
- `agent_docs/snapshot.md` — строка о заведённом каталоге и обновлённая
  строка про отбор тестов CI.

## Зачем

ADR `2026-09-23-0646` решил делить работу иначе — сервис и роутер отдельно
от дня. Гейт атласа это опроверг: инструмент связывает документ с единицей по
имени файла, имя ADR содержит `day15`, каталога `days/day15/` не было —
`--check` красный (`lib/extract.js:452` инструмента). Оверлей `atlas/overlay.json`
не подходит: его записи — постоянные исключения с обоснованием, а не отсрочка.
Значит ADR обязан приехать вместе с каталогом.

Заодно копия в 4 947 строк вынесена из диффа логики: механическую замену
номера ревьюер проверяет одной командой, а следующий PR несёт только
содержательные правки — промпты профиля, этап `prepare` и потолок 32 000.

Новой логики здесь нет: до следующего PR по адресу `/day15/` работает день 14
на своих cookie и своём журнале. Карточка лендинга при этом уже называет
предмет дня 15 по ADR — расхождение названо в описании PR и закрывается
мержем следующего PR.

## Обновлено

- [x] agent_docs/snapshot.md
- [ ] agent_docs/backlog.md / backlog-closed.md — нечего переносить
- [x] Тесты — 101 тест `days/day15` зелёные без правок (копия тестов дня 14)
- [x] Документация — `deploy/.env.example`, `days/day15/README.md` (копия)

## Связанные решения

- `agent_docs/adr/2026-09-23-0646-day15-profile-prompts-prepare-stage.md`
- `agent_docs/adr/2026-09-22-0827-day14-profile-invariants.md` — день, с которого снята копия
- `agent_docs/guides/verification.md`

## Следующие шаги

- Владельцу: положить на сервере `deploy/day15.env` с `AGENT_KEY` —
  копией значения из `deploy/agents.env`, права `600`. Без файла контейнер
  поднимается, но `/healthz` отвечает 503, и шаг проверки живого сайта
  в выкатке краснеет.
- Следующим PR — содержание дня 15 по ADR `2026-09-23-0646`: таблицы
  `profile_prompts` и `run_prompts`, опции `prompts` и `stages`, этап
  `prepare`, `STAGED15_MAX_TOKENS`, окно «Об агенте», README дня.
- Текст `days/day15/README.md` пока описывает день 14 дословно (агент
  `invariant-agent`, ADR дня 14, именное исключение корпуса на дни 13 и 14) —
  переписывается тем же PR.
