# challenge.zpq.ai — витрина учебного челленджа, индексация не нужна.
#
# Запрет индексации даёт заголовок X-Robots-Tag: noindex на всех ответах
# (deploy/Caddyfile). Обход НАМЕРЕННО не запрещаем: запретив краулинг здесь,
# мы лишили бы робота возможности увидеть noindex — и голый URL мог бы попасть
# в выдачу без содержимого. Чтобы страница исчезла из индекса, робот обязан
# её скачать и прочитать заголовок.

User-agent: *
Allow: /
