<!-- Заголовок PR: «[класс A] тип: суть». Класс — по AGENTS.md, «Роли агентов»;
     сомнение — в сторону старшего класса. Если изменение опирается на ADR —
     ссылка на файл ADR ниже. -->

## Что сделано

<!-- Одна-две строки: что меняется и зачем. -->

<!-- Три раздела ниже обязательны для PR класса A: гейт docs-guard требует,
     чтобы они присутствовали и были непусты (ADR 2026-09-24-1230, A1).
     Шаг проверяет присутствие, а не правду написанного, — правду проверяет
     гейт. В PR классов B и C разделы можно удалить. -->

## Что держит

<!-- Строка (файл:строка или имя шага), которая держит каждое новое правило
     безопасности или расхода; что сломается, если убрать именно её;
     путь законного клиента через защиту — команда и результат. -->

## Красная ветвь

<!-- Для каждого нового теста или шага CI: мутация, команда, красный результат.
     Мутация бьёт в исполняемую строку, а не рядом. -->

## Стенд ≠ прод

<!-- Что в стенде подменено и чем доказано, что запрос дошёл до стенда:
     запись в его журнале, не код ответа. -->
